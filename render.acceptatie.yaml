# Jobsy ACCEPTATIE Blueprint (Render project "Lobsy Acceptatie").
# Docs: docs/deploy-render.md § Acceptatie
#
# Same stack as production (Starter web + Basic Postgres, Frankfurt, Docker),
# but UNIQUE resource names so this cannot attach to Lobsy prod.
#
# Dashboard: New → Blueprint → repo Jobsy → branch Acceptatie →
# Blueprint Path: render.acceptatie.yaml
#
# Never point this file at jobsy-api / jobsy-web / jobsy-db.

projects:
  - name: Lobsy Acceptatie
    environments:
      - name: Acceptatie
        databases:
          - name: jobsy-db-acceptatie
            plan: basic-256mb
            region: frankfurt
            postgresMajorVersion: "16"
            databaseName: jobsy
            user: jobsy

        services:
          - type: web
            name: jobsy-api-acceptatie
            runtime: docker
            plan: starter
            region: frankfurt
            branch: Acceptatie
            dockerfilePath: ./Jobsy.Api/Dockerfile
            dockerContext: .
            healthCheckPath: /health
            envVars:
              - key: ASPNETCORE_ENVIRONMENT
                value: Production
              - key: DOTNET_HOSTBUILDER__RELOADCONFIGONCHANGE
                value: "false"
              - key: AllowedHosts
                value: "*"
              - key: JobsyAuth__AllowDevelopmentAuth
                value: "true"
              - key: JobsyAuth__AllowStubPayments
                value: "true"
              - key: JobsyAuth__DevelopmentAuthSecret
                generateValue: true
              - key: JobsyAuth__LocalSessionSigningKey
                generateValue: true
              - key: JobsyAuth__ExternalProvisionSecret
                generateValue: true
              - key: JobsyFeatures__ExposeRegistrationActivationLinks
                value: "false"
              - key: Swagger__Enabled
                value: "false"
              - key: VerificationCodes__Pepper
                generateValue: true
              - key: ConnectionStrings__JobsyDb
                fromDatabase:
                  name: jobsy-db-acceptatie
                  property: connectionString
              - key: PublicWebBaseUrl
                fromService:
                  name: jobsy-web-acceptatie
                  type: web
                  envVarKey: RENDER_EXTERNAL_URL
              - key: PublicApiBaseUrl
                fromService:
                  name: jobsy-api-acceptatie
                  type: web
                  envVarKey: RENDER_EXTERNAL_URL
              - key: Cors__AllowedOrigins__0
                fromService:
                  name: jobsy-web-acceptatie
                  type: web
                  envVarKey: RENDER_EXTERNAL_URL
              - key: Mail__ResendApiKey
                sync: false
              - key: Mail__FromAddress
                sync: false

          - type: web
            name: jobsy-web-acceptatie
            runtime: docker
            plan: starter
            region: frankfurt
            branch: Acceptatie
            dockerfilePath: ./Jobsy.Web/Dockerfile
            dockerContext: .
            healthCheckPath: /
            envVars:
              - key: ASPNETCORE_ENVIRONMENT
                value: Production
              - key: DOTNET_HOSTBUILDER__RELOADCONFIGONCHANGE
                value: "false"
              - key: AllowedHosts
                value: "*"
              - key: JobsyAuth__AllowDevelopmentAuth
                value: "true"
              - key: JobsyAuth__DevelopmentAuthSecret
                fromService:
                  name: jobsy-api-acceptatie
                  type: web
                  envVarKey: JobsyAuth__DevelopmentAuthSecret
              - key: JobsyAuth__LocalSessionSigningKey
                fromService:
                  name: jobsy-api-acceptatie
                  type: web
                  envVarKey: JobsyAuth__LocalSessionSigningKey
              - key: JobsyAuth__ExternalProvisionSecret
                fromService:
                  name: jobsy-api-acceptatie
                  type: web
                  envVarKey: JobsyAuth__ExternalProvisionSecret
              - key: ApiBaseUrl
                fromService:
                  name: jobsy-api-acceptatie
                  type: web
                  envVarKey: RENDER_EXTERNAL_URL
              - key: ConnectionStrings__JobsyDb
                fromDatabase:
                  name: jobsy-db-acceptatie
                  property: connectionString
